<div class="items">
<% @list_of_items.each do |item| %>
  <div class="item link">
    <a href="<%= item.link_url %>" class="thumbnail">
      <%= item.link_description %>
    </a>
  </div>
<% end %>
</div>
